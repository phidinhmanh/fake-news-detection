# ── Shared dependencies (tất cả người dùng chung) ─────
pydantic>=2.0
python-dotenv>=1.0
pytest>=8.0
pytest-cov>=5.0

# ── Người A: Data Pipeline ────────────────────────────
# pip install -r requirements-data.txt
scrapy>=2.11
underthesea>=6.8
pandas>=2.2
pyarrow>=15.0
fastapi>=0.110
uvicorn[standard]>=0.29
scikit-learn>=1.4
datasets>=2.18              # HuggingFace datasets

# ── Người B: Model Training ───────────────────────────
# pip install -r requirements-model.txt
torch>=2.2
transformers>=4.39
peft>=0.10                  # LoRA adapter
lightning>=2.2              # PyTorch Lightning
shap>=0.45
accelerate>=0.28
evaluate>=0.4
sentencepiece>=0.2          # Tokenizer backend

# ── Người C: Streamlit UI ─────────────────────────────
# pip install -r requirements-ui.txt
streamlit>=1.32
requests>=2.31
plotly>=5.20
